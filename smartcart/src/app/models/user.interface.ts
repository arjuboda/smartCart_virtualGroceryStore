export interface User {
    id: number;
    username: string;
    email: string;
    mobile_number: number;
    // ... other user properties from API response
    // user_addresses: any[]; // Address array with specific structure
}
